{
  "name": "suviet360",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run test --workspace server",
    "dev": "concurrently \"npm run dev --workspace client\" \"npm run dev --workspace server\" \"npm run dev --workspace socket-server\"",
    "build": "npm run build --workspace client",
    "lint": "npm run lint --workspace client",
    "format": "prettier --check ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "socket-server"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.3"
  }
}
